---
phase: 03-polish-and-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RestaurantList.tsx
  - src/components/RouletteWheel.tsx
  - src/App.tsx
  - src/index.css
autonomous: false
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Restaurant list cards appear below the roulette wheel showing name, rating, distance, price"
    - "Each restaurant card links to Google Maps on click"
    - "Roulette wheel resizes responsively between 280px and 400px based on viewport"
    - "Loading spinner with Korean text appears during location detection"
    - "Loading spinner appears during restaurant data fetch"
    - "Layout works on mobile (320px+) and desktop with clean spacing"
  artifacts:
    - path: "src/components/RestaurantList.tsx"
      provides: "Compact restaurant card list"
      contains: "google.com/maps"
    - path: "src/components/RouletteWheel.tsx"
      provides: "Responsive wheel sizing"
      contains: "min-w|max-w"
    - path: "src/App.tsx"
      provides: "Loading states, restaurant list integration, responsive layout"
      contains: "animate-spin"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/RestaurantList.tsx"
      via: "JSX import and render"
      pattern: "<RestaurantList"
    - from: "src/components/RestaurantList.tsx"
      to: "google.com/maps"
      via: "anchor href"
      pattern: "google.com/maps"
---

<objective>
Add restaurant list view, make layout fully responsive for mobile, add proper loading states with spinners, and polish the overall visual design for a clean, minimal look.

Purpose: Delivers the complete polished E2E experience on mobile and desktop with proper feedback during async operations.
Output: New RestaurantList component, updated App.tsx with loading states and responsive layout, responsive RouletteWheel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-and-integration/03-CONTEXT.md
@.planning/phases/02-core-interaction/02-01-SUMMARY.md
@.planning/phases/02-core-interaction/02-02-SUMMARY.md
@.planning/phases/02-core-interaction/02-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/restaurant.ts:
```typescript
export type Category = 'korean' | 'chinese' | 'japanese' | 'western' | 'other';
export interface Restaurant {
  id: string;
  name: string;
  rating: number;
  priceLevel: number;
  location: Location;
  address: string;
  distance?: number;
  types: string[];
  category: Category;
}
```

From src/components/RouletteWheel.tsx:
```typescript
interface RouletteWheelProps {
  restaurants: Restaurant[];
  onResult: (winner: Restaurant) => void;
  isVisible: boolean;
}
```

From src/hooks/useLocation.ts:
```typescript
export function useLocation(): {
  location: Location | null;
  error: string | null;
  isLoading: boolean;
}
```

From src/hooks/useRestaurants.ts:
```typescript
export function useRestaurants(lat, lng, category): {
  restaurants: Restaurant[];
  isLoading: boolean;
  error: string | null;
}
```

From src/constants/categories.ts:
```typescript
export const CATEGORIES: CategoryInfo[] = [...];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RestaurantList component and update RouletteWheel for responsive sizing</name>
  <files>src/components/RestaurantList.tsx, src/components/RouletteWheel.tsx</files>
  <read_first>src/components/ResultCard.tsx, src/components/RouletteWheel.tsx, src/types/restaurant.ts, src/constants/categories.ts</read_first>
  <action>
**Create `src/components/RestaurantList.tsx` (per D-04, D-05, D-06):**

A compact card list component that displays restaurants below the roulette wheel.

```typescript
import type { Restaurant } from '../types/restaurant';
import { CATEGORIES } from '../constants/categories';

interface RestaurantListProps {
  restaurants: Restaurant[];
}

export function RestaurantList({ restaurants }: RestaurantListProps) {
  if (restaurants.length === 0) return null;

  return (
    <div className="w-full max-w-md mt-6">
      <h2 className="text-sm font-semibold text-gray-500 mb-3 px-1">
        주변 식당 ({restaurants.length}개)
      </h2>
      <div className="space-y-2">
        {restaurants.map((r) => {
          const cat = CATEGORIES.find(c => c.id === r.category);
          const emoji = cat?.emoji ?? '';
          const mapsUrl = `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(r.name + ' ' + r.address)}`;
          const stars = '★'.repeat(Math.round(r.rating)) + '☆'.repeat(5 - Math.round(r.rating));
          const price = '₩'.repeat(r.priceLevel);

          return (
            <a
              key={r.id}
              href={mapsUrl}
              target="_blank"
              rel="noopener noreferrer"
              className="block bg-white rounded-xl p-3 shadow-sm border border-gray-100 hover:shadow-md hover:border-gray-200 transition-all active:scale-[0.98]"
            >
              <div className="flex items-start justify-between">
                <div className="flex-1 min-w-0">
                  <p className="font-semibold text-gray-900 text-sm truncate">
                    {emoji} {r.name}
                  </p>
                  <div className="flex items-center gap-2 mt-1 text-xs text-gray-500">
                    <span className="text-yellow-500">{stars}</span>
                    <span>{r.rating.toFixed(1)}</span>
                  </div>
                </div>
                <div className="text-right flex-shrink-0 ml-2">
                  <p className="text-xs text-gray-500">
                    {r.distance !== undefined ? `${r.distance}m` : ''}
                  </p>
                  {r.priceLevel > 0 && (
                    <p className="text-xs text-green-600 font-medium mt-0.5">{price}</p>
                  )}
                </div>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  );
}
```

**Update `src/components/RouletteWheel.tsx` for responsive sizing (per D-07):**

Change the wheel container from fixed `max-w-[340px]` to responsive sizing: `w-[min(85vw,400px)] min-w-[280px]`. Apply this to both the canvas container div and the button.

Replace:
- `className="w-full max-w-[340px] aspect-square mx-auto"` with `className="w-[min(85vw,400px)] min-w-[280px] aspect-square mx-auto"`
- Same for the empty state container
- Button: `className="mt-6 w-[min(85vw,400px)] min-w-[280px] mx-auto block ..."`
  </action>
  <verify>
    <automated>cd /Users/n-mjkim/workspace2/test_0408 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/components/RestaurantList.tsx
    - grep -q "google.com/maps" src/components/RestaurantList.tsx
    - grep -q "rating" src/components/RestaurantList.tsx
    - grep -q "distance" src/components/RestaurantList.tsx
    - grep -q "priceLevel\|₩" src/components/RestaurantList.tsx
    - grep -q "min(85vw" src/components/RouletteWheel.tsx
    - grep -q "min-w-\[280px\]" src/components/RouletteWheel.tsx
  </acceptance_criteria>
  <done>RestaurantList renders compact cards with name, emoji, rating stars, distance, price, and Google Maps link. RouletteWheel canvas resizes responsively between 280px-400px based on viewport width.</done>
</task>

<task type="auto">
  <name>Task 2: Polish App.tsx with loading states, restaurant list, and responsive design</name>
  <files>src/App.tsx, src/index.css</files>
  <read_first>src/App.tsx, src/index.css, src/components/RestaurantList.tsx</read_first>
  <action>
**Rewrite `src/App.tsx` with full polish (per D-07, D-08, D-09, D-10, D-11, D-12):**

Key changes:
1. **Loading spinner for location (D-10):** Replace plain text with animated spinner + "위치를 찾고 있어요..." text
2. **Loading spinner for restaurants (D-11):** Replace plain text with animated spinner + "식당을 찾고 있어요..."
3. **Restaurant list (D-04):** Add `<RestaurantList>` below the roulette wheel
4. **Responsive layout (D-07, D-09):** Use `min-h-dvh` (already there), ensure `safe-area-inset` padding
5. **Design polish (D-12):** Consistent spacing, clean typography, proper section gaps

Full App.tsx structure:

```tsx
import { useState } from 'react';
import { useLocation } from './hooks/useLocation';
import { useRestaurants } from './hooks/useRestaurants';
import { CategorySelector } from './components/CategorySelector';
import { RouletteWheel } from './components/RouletteWheel';
import { ResultCard } from './components/ResultCard';
import { RestaurantList } from './components/RestaurantList';
import type { Category, Restaurant } from './types/restaurant';

function LoadingSpinner({ message }: { message: string }) {
  return (
    <div className="flex items-center gap-2 text-gray-400 text-sm py-2">
      <svg className="animate-spin h-4 w-4" viewBox="0 0 24 24" fill="none">
        <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
        <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
      </svg>
      <span>{message}</span>
    </div>
  );
}

function App() {
  const [selectedCategory, setSelectedCategory] = useState<Category | null>(null);
  const [winner, setWinner] = useState<Restaurant | null>(null);

  const { location, error: locationError, isLoading: locationLoading } = useLocation();
  const { restaurants, isLoading: restaurantsLoading, error: restaurantsError } = useRestaurants(
    location?.lat ?? null,
    location?.lng ?? null,
    selectedCategory
  );

  return (
    <div className="min-h-dvh bg-gray-50 flex flex-col items-center px-4 py-6 pb-safe">
      {/* Header */}
      <h1 className="text-2xl font-bold text-gray-900 mb-1">점심 룰렛</h1>
      <p className="text-sm text-gray-400 mb-6">오늘 뭐 먹지?</p>

      {/* Location status */}
      {locationLoading && <LoadingSpinner message="위치를 찾고 있어요..." />}
      {locationError && <p className="text-xs text-gray-400 mb-2">{locationError}</p>}

      {/* Category selector */}
      <div className="w-full max-w-md mb-6">
        <CategorySelector
          selectedCategory={selectedCategory}
          onSelect={(cat) => {
            setSelectedCategory(cat);
            setWinner(null);
          }}
        />
      </div>

      {/* Loading state for restaurants */}
      {restaurantsLoading && <LoadingSpinner message="식당을 찾고 있어요..." />}
      {restaurantsError && (
        <p className="text-red-500 text-sm mb-4">{restaurantsError}</p>
      )}

      {/* Roulette wheel */}
      <RouletteWheel
        restaurants={restaurants}
        onResult={(w) => setWinner(w)}
        isVisible={selectedCategory !== null && !restaurantsLoading}
      />

      {/* Restaurant list below wheel */}
      {selectedCategory && !restaurantsLoading && (
        <RestaurantList restaurants={restaurants} />
      )}

      {/* Result modal */}
      {winner && (
        <ResultCard
          restaurant={winner}
          onClose={() => setWinner(null)}
        />
      )}
    </div>
  );
}

export default App;
```

**Update `src/index.css`** if needed: Ensure Tailwind import exists. Add safe area padding utility if not covered by Tailwind:

```css
@import "tailwindcss";

/* Safe area insets for iOS notch devices */
.pb-safe {
  padding-bottom: env(safe-area-inset-bottom, 0px);
}
```

Note: If `@import "tailwindcss"` already exists in index.css, just add the `.pb-safe` class below it. Do NOT duplicate the import.
  </action>
  <verify>
    <automated>cd /Users/n-mjkim/workspace2/test_0408 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "animate-spin" src/App.tsx
    - grep -q "위치를 찾고 있어요" src/App.tsx
    - grep -q "식당을 찾고 있어요" src/App.tsx
    - grep -q "RestaurantList" src/App.tsx
    - grep -q "LoadingSpinner" src/App.tsx
    - grep -q "min-h-dvh" src/App.tsx
    - grep -q "오늘 뭐 먹지" src/App.tsx
  </acceptance_criteria>
  <done>App.tsx has animated loading spinners for location and restaurant fetch, integrates RestaurantList below wheel, uses dvh for mobile viewport, has clean minimal design with consistent spacing and subtitle. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete E2E flow on mobile and desktop</name>
  <files>n/a</files>
  <action>Human verification checkpoint. Run `npm run dev` and test the complete flow in browser per the verification steps below.</action>
  <what-built>Complete polished flow: category selection, roulette spin, result modal, restaurant list, responsive layout, loading states</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173 in desktop browser
    2. Verify "점심 룰렛" header with "오늘 뭐 먹지?" subtitle appears
    3. Verify loading spinner appears briefly during location detection
    4. Select a category chip (e.g., 한식)
    5. Verify loading spinner appears while restaurants load
    6. Verify roulette wheel appears with restaurant segments
    7. Verify restaurant list cards appear below the wheel with name, rating, distance, price
    8. Click a restaurant card — verify it opens Google Maps in new tab
    9. Click "돌리기!" — verify wheel spins and stops smoothly
    10. Verify result modal appears with restaurant details
    11. Close modal and select a different category — verify wheel and list update
    12. Open Chrome DevTools, toggle device toolbar (mobile 375px width)
    13. Verify wheel resizes appropriately (not cut off, not tiny)
    14. Verify category chips are horizontally scrollable
    15. Verify overall spacing and typography look clean and consistent
  </how-to-verify>
  <verify>
    <automated>cd /Users/n-mjkim/workspace2/test_0408 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User approves the complete E2E flow on both desktop and mobile viewports</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Restaurant data -> RestaurantList | Untrusted name/address used in Google Maps URL |
| External links | User clicks link to google.com/maps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | S (Spoofing) | RestaurantList maps URL | mitigate | Use encodeURIComponent on name+address before embedding in URL |
| T-03-04 | I (Info Disclosure) | RestaurantList | accept | Only displays public restaurant data, no PII involved |
</threat_model>

<verification>
- `npm run build` succeeds with zero errors
- RestaurantList component exists and renders cards with maps links
- Loading spinners appear during location and restaurant loading
- Wheel resizes responsively between 280px-400px
- Layout works at 375px mobile width
</verification>

<success_criteria>
- Complete category -> spin -> result flow works without refresh
- Mobile layout (375px) shows properly sized wheel, scrollable chips, readable cards
- Loading spinners with Korean messages appear during async operations
- Restaurant list below wheel shows all category restaurants with name, rating, distance, price
- Clean, minimal visual design with consistent spacing
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-and-integration/03-02-SUMMARY.md`
</output>
