---
phase: 01-foundation-and-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/index.css
  - src/App.tsx
  - src/vite-env.d.ts
  - src/types/restaurant.ts
  - src/constants/categories.ts
  - src/services/mockData.ts
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - LOC-03
must_haves:
  truths:
    - "The Vite dev server starts without errors and renders a React page"
    - "TypeScript compiles with zero errors"
    - "Tailwind CSS utility classes render correctly"
    - "Mock restaurant data is available as an importable module with ~10 entries"
    - "Restaurant type defines the canonical data shape used across the app"
  artifacts:
    - path: "src/types/restaurant.ts"
      provides: "Restaurant, Location, Category type definitions"
      exports: ["Restaurant", "Location", "Category", "CATEGORIES"]
    - path: "src/services/mockData.ts"
      provides: "Mock restaurant data (~10 entries)"
      exports: ["MOCK_RESTAURANTS", "MockDataProvider"]
    - path: "src/constants/categories.ts"
      provides: "Category list definitions"
      exports: ["CATEGORIES"]
    - path: "vite.config.ts"
      provides: "Vite config with React + Tailwind plugins"
  key_links:
    - from: "src/services/mockData.ts"
      to: "src/types/restaurant.ts"
      via: "imports Restaurant type"
      pattern: "import.*Restaurant.*from.*types/restaurant"
    - from: "src/constants/categories.ts"
      to: "src/types/restaurant.ts"
      via: "imports Category type"
      pattern: "import.*Category.*from.*types/restaurant"
---

<objective>
Scaffold the Vite + React + TypeScript + Tailwind CSS v4 project, define canonical types, create category constants, and build the mock data provider with ~10 Korean restaurant entries.

Purpose: Establish the project foundation and the mock data fallback (LOC-03) so the app always works regardless of API key availability.
Output: A running dev server with typed mock data ready for consumption by hooks and services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript + Tailwind project and define types</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/index.css, src/App.tsx, src/vite-env.d.ts, src/types/restaurant.ts, .env.example, .gitignore</files>
  <read_first>.planning/research/STACK.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>
1. Create the project using `npm create vite@latest . -- --template react-ts` (in current directory since it already exists). If the command requires an empty directory, create a temp directory, scaffold there, and move files back.

2. Install dependencies:
   ```bash
   npm install tailwindcss @tailwindcss/vite
   ```

3. Update `vite.config.ts` to:
   ```typescript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'
   import tailwindcss from '@tailwindcss/vite'

   export default defineConfig({
     plugins: [react(), tailwindcss()],
   })
   ```

4. Replace `src/index.css` content with:
   ```css
   @import "tailwindcss";
   ```

5. Create `src/types/restaurant.ts` with these exact type definitions:
   ```typescript
   export interface Location {
     lat: number;
     lng: number;
   }

   export interface Restaurant {
     id: string;
     name: string;
     rating: number;        // 1.0 - 5.0
     priceLevel: number;    // 1-4 (1=cheap, 4=expensive)
     location: Location;
     address: string;
     distance?: number;     // meters from user, computed client-side
     types: string[];       // e.g. ['korean', 'restaurant']
     category: Category;
   }

   export type Category = 'korean' | 'chinese' | 'japanese' | 'western' | 'other';

   export interface CategoryInfo {
     id: Category;
     label: string;         // Korean display name
     emoji: string;
   }
   ```

6. Create `.env.example` with:
   ```
   VITE_GOOGLE_PLACES_API_KEY=your_api_key_here
   ```

7. Ensure `.gitignore` includes `.env` and `node_modules/`.

8. Replace `src/App.tsx` with a minimal component that renders a Tailwind-styled heading:
   ```typescript
   function App() {
     return (
       <div className="min-h-screen bg-gray-50 flex items-center justify-center">
         <h1 className="text-3xl font-bold text-gray-900">Lunch Roulette</h1>
       </div>
     );
   }
   export default App;
   ```

9. Clean up Vite template files: remove `src/App.css`, `src/assets/` directory, and any default Vite SVG logo references.
  </action>
  <verify>
    <automated>cd /Users/n-mjkim/workspace2/test_0408 && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/restaurant.ts` exports `Restaurant`, `Location`, `Category`, `CategoryInfo` interfaces/types
    - `vite.config.ts` contains both `react()` and `tailwindcss()` in plugins array
    - `src/index.css` contains `@import "tailwindcss"`
    - `.env.example` contains `VITE_GOOGLE_PLACES_API_KEY`
    - `.gitignore` contains `.env`
    - `npm run build` completes with exit code 0
    - `npx tsc --noEmit` completes with exit code 0
  </acceptance_criteria>
  <done>Vite + React + TS + Tailwind project builds cleanly, Restaurant type is defined and exported, .env.example exists</done>
</task>

<task type="auto">
  <name>Task 2: Create category constants and mock data provider</name>
  <files>src/constants/categories.ts, src/services/mockData.ts</files>
  <read_first>src/types/restaurant.ts</read_first>
  <action>
1. Create `src/constants/categories.ts`:
   ```typescript
   import type { CategoryInfo } from '../types/restaurant';

   export const CATEGORIES: CategoryInfo[] = [
     { id: 'korean', label: '한식', emoji: '🍚' },
     { id: 'chinese', label: '중식', emoji: '🥟' },
     { id: 'japanese', label: '일식', emoji: '🍣' },
     { id: 'western', label: '양식', emoji: '🍝' },
     { id: 'other', label: '기타', emoji: '🍽️' },
   ];
   ```

2. Create `src/services/mockData.ts` with exactly 10 mock restaurants near Gangnam Station (lat: 37.4979, lng: 127.0276). Each restaurant must conform to the `Restaurant` type. Include a mix of categories:
   - 3 Korean restaurants (e.g., 김치찌개집, 한솥도시락, 본죽)
   - 2 Chinese restaurants (e.g., 홍콩반점, 양자강)
   - 2 Japanese restaurants (e.g., 스시로, 이찌방라멘)
   - 2 Western restaurants (e.g., 맘스터치, 빕스)
   - 1 Other restaurant (e.g., 써브웨이)

   Each entry must have: id (string, e.g., "mock-1"), name, rating (1.0-5.0), priceLevel (1-4), location (lat/lng near 37.498, 127.028), address (realistic Gangnam addresses), types array, and category.

3. Export a `MockDataProvider` class with a `searchNearby` method:
   ```typescript
   export class MockDataProvider {
     async searchNearby(
       _lat: number,
       _lng: number,
       category?: Category
     ): Promise<Restaurant[]> {
       // Simulate network delay
       await new Promise(resolve => setTimeout(resolve, 300));

       if (category) {
         return MOCK_RESTAURANTS.filter(r => r.category === category);
       }
       return [...MOCK_RESTAURANTS];
     }
   }
   ```

4. Also export `MOCK_RESTAURANTS` array directly for testing/debugging.
  </action>
  <verify>
    <automated>cd /Users/n-mjkim/workspace2/test_0408 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/constants/categories.ts` exports `CATEGORIES` array with 5 entries (korean, chinese, japanese, western, other)
    - `src/services/mockData.ts` exports `MOCK_RESTAURANTS` array with exactly 10 entries
    - `src/services/mockData.ts` exports `MockDataProvider` class with `searchNearby` method
    - Every mock restaurant has all required `Restaurant` fields: id, name, rating, priceLevel, location, address, types, category
    - `grep -c "id:" src/services/mockData.ts` returns 10 or more (one per restaurant)
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>10 mock restaurants with realistic Korean data are available via MockDataProvider.searchNearby(), category constants define 5 food categories with Korean labels</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file | API key stored in environment variable, must not be committed to git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file | mitigate | .gitignore includes .env, .env.example has placeholder only |
| T-01-02 | Information Disclosure | VITE_ env vars | accept | Client-side SPA -- API key will be in bundle. Acceptable for demo project. Production would need backend proxy. |
</threat_model>

<verification>
1. `npm run dev` starts dev server without errors
2. `npm run build` completes successfully
3. `npx tsc --noEmit` shows zero TypeScript errors
4. Browser shows "Lunch Roulette" heading with Tailwind styling at localhost
5. `src/services/mockData.ts` has 10 restaurant entries importable with correct types
</verification>

<success_criteria>
- Vite dev server runs and renders React app with Tailwind styling
- TypeScript compiles with zero errors
- Restaurant type, CategoryInfo type, 5 categories, and 10 mock restaurants are defined and exported
- .env.example documents the API key variable
- .gitignore protects .env from being committed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-layer/01-01-SUMMARY.md`
</output>
